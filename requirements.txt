# ALRL — minimal pinned requirements.
# Tested on macOS 14 / Linux Ubuntu 22.04, Python 3.10.
# Install with:  pip install -r requirements.txt

# Core deep-learning stack
torch>=2.0
numpy>=1.24
scikit-learn>=1.3

# Plotting & data IO
matplotlib>=3.7
pandas>=2.0           # used by analysis utilities

# Optional: nicer task-relationship graphs in fig10b
networkx>=3.1

# Notebook / interactive exploration (optional)
jupyterlab>=4.0       # only required if you want to play with the demo notebooks
